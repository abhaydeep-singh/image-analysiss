ollama pull moondream
pip install ollama Pillow reportlab requests
python -m pip install ollama Pillow reportlab requests


openclip
pip install open_clip_torch torch torchvision



pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121 for 1650
<!-- Instruction -->
exe clip.py to create index/embeddings
query.py for embeddings search only
main.py for embeddings + monndream analysis 