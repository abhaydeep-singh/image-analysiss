# Core ML
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

open_clip_torch==3.3.0
transformers==4.40.0      # DO NOT upgrade — performance regression in newer versions
einops==0.8.2

# Vector DB
chromadb==1.5.5

# Image processing
Pillow==12.0.0
numpy==2.2.6

# PDF generation
reportlab==4.4.10

# Monitoring (CPU safe)
psutil==7.2.2

# HTTP (Ollama / downloads)
requests


# NOTES:
# torch CPU build installs automatically from PyPI
#
# Moondream runs via transformers (not moondream pip package)
# First run downloads model (~3.7GB) to HuggingFace cache
#
# For faster CPU inference you can enable:
#   torch.set_num_threads(N)
#
# Recommended:
#   Python 3.10
#
# Windows encoding fix sometimes required:
#   import sys
#   sys.stdout.reconfigure(encoding="utf-8")